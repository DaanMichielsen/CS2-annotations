// Web stub — Vite's import.meta.glob is not available in Next.js/webpack.
// Map canvas images are not bundled; consumers fall back to null.
export function getMapOverviewUrl(_fileName: string): string | null { return null }
export function getMapIconUrl(_mapName: string): string | null { return null }
